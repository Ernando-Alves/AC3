<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login de Cliente - Don Python</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: ui-serif, Georgia, Cambria, "Times New Roman", Times, serif; }
        .btn-premium { background-color: #A37A3D; color: #FFFFFF; transition: all 0.2s; }
        .btn-premium:hover { background-color: #8C6A34; }
    </style>
</head>
<body class="bg-stone-100 min-h-screen flex flex-col antialiased">

    <header class="bg-stone-900 text-white shadow-xl">
        <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
            <h1 class="text-3xl font-bold tracking-wider text-amber-500">Don Python</h1>
            <a href="/" class="btn-premium font-semibold px-5 py-2 rounded-full shadow-lg">Voltar ao Cardápio</a>
        </div>
    </header>

    <main class="flex-grow flex flex-col justify-center items-center py-16">
        <!-- Área de Flashes (Para mostrar mensagens de login/erro) -->
        {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            <div class="mb-6 space-y-2 w-full max-w-md">
                {% for category, msg in messages %}
                    <div class="p-4 rounded-lg text-white font-medium 
                        {% if category == 'success' %}bg-green-700
                        {% elif category == 'danger' or category == 'info' %}bg-red-700
                        {% else %}bg-gray-700{% endif %} shadow-md">
                        {{ msg }}
                    </div>
                {% endfor %}
            </div>
        {% endif %}
        {% endwith %}

        <div class="bg-white shadow-2xl rounded-2xl w-full max-w-md p-10 border-t-4 border-amber-600">
            <h2 class="text-3xl font-bold text-center text-stone-900 mb-8">Login de Cliente</h2>

            <form action="{{ url_for('main.login') }}" method="post" class="space-y-6">
                <div>
                    <label for="email" class="block text-stone-700 font-medium mb-2">E-mail</label>
                    <input type="email" id="email" name="email" required
                        class="w-full border border-stone-300 rounded-lg px-4 py-3 focus:ring-amber-600 focus:border-amber-600 focus:outline-none">
                </div>
                <div>
                    <label for="senha" class="block text-stone-700 font-medium mb-2">Senha</label>
                    <input type="password" id="senha" name="senha" required
                        class="w-full border border-stone-300 rounded-lg px-4 py-3 focus:ring-amber-600 focus:border-amber-600 focus:outline-none">
                </div>

                <div class="pt-4 text-center">
                    <button type="submit"
                        class="w-full btn-premium px-6 py-3 rounded-lg hover:bg-amber-700 transition font-bold shadow-lg">Entrar</button>
                    <p class="mt-4 text-stone-600">Não tem conta? <a href="{{ url_for('main.cadastro_cliente') }}" class="text-amber-600 hover:underline font-semibold">Cadastre-se aqui</a></p>
                </div>
            </form>
        </div>
    </main>

    <footer class="bg-stone-900 text-stone-400 text-center py-6 mt-16">
        <p class="text-lg font-serif">Pizzaria Don Python. Tradição e Sabor.</p>
        <p class="text-sm mt-1">© 2025. Todos os direitos reservados.</p>
    </footer>

</body>
</html>